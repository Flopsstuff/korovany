import{_ as e,o as s,c as t,a2 as n}from"./chunks/framework.BYlK8ftn.js";const k=JSON.parse('{"title":"Caravans — \\"грабить корованы\\" (E3.3)","description":"","frontmatter":{},"headers":[],"relativePath":"guide/caravans.md","filePath":"guide/caravans.md","lastUpdated":1781996558000}'),i={name:"guide/caravans.md"};function o(d,a,l,r,h,p){return s(),t("div",null,[...a[0]||(a[0]=[n(`<h1 id="caravans-—-грабить-корованы-e3-3" tabindex="-1">Caravans — &quot;грабить корованы&quot; (E3.3) <a class="header-anchor" href="#caravans-—-грабить-корованы-e3-3" aria-label="Permalink to &quot;Caravans — &quot;грабить корованы&quot; (E3.3)&quot;">​</a></h1><p>The signature loot loop: wandering caravans the player ambushes for loot. A caravan is a <strong>non-combatant loot piñata</strong> — it follows a looping path, flees when ambushed, and on defeat rolls a loot table and emits a reward event.</p><p>Pure game logic lives in <code>src/game/ai/caravanFSM.ts</code> (the FSM), <code>src/game/loot/</code> (loot tables), and <code>src/game/util/seededRandom.ts</code> (the seeded PRNG). The Babylon mesh wrapper is <code>src/scenes/caravanEnemy.ts</code>.</p><h2 id="behaviour-states" tabindex="-1">Behaviour states <a class="header-anchor" href="#behaviour-states" aria-label="Permalink to &quot;Behaviour states&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>wander ──[player inside ambushRadius]──────▶ flee</span></span>
<span class="line"><span>wander ──[takes a melee hit]───────────────▶ flee</span></span>
<span class="line"><span>flee   ──[player beyond calmRadius]────────▶ wander</span></span>
<span class="line"><span>any    ──[HP == 0]─────────────────────────▶ dead  (terminal → rolls loot)</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Phase</th><th>Behaviour</th></tr></thead><tbody><tr><td><strong>wander</strong></td><td>Follow the looping waypoint path at 1.2 m/s</td></tr><tr><td><strong>flee</strong></td><td>Run directly away from the player at 2.6 m/s</td></tr><tr><td><strong>dead</strong></td><td>Stop; roll the loot table once and emit the drop</td></tr></tbody></table><p>The caravan deals <strong>no damage</strong> — the player defeats it with the existing <a href="./melee-combat">E2 melee</a>. Being struck always springs the ambush, so a sneak hit from outside <code>ambushRadius</code> still makes it flee.</p><h2 id="default-parameters" tabindex="-1">Default parameters <a class="header-anchor" href="#default-parameters" aria-label="Permalink to &quot;Default parameters&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Max HP</td><td>80</td></tr><tr><td>Ambush radius</td><td>6 m (player closer → flee)</td></tr><tr><td>Calm radius</td><td>16 m (player farther → back to wander)</td></tr><tr><td>Wander speed</td><td>1.2 m/s</td></tr><tr><td>Flee speed</td><td>2.6 m/s</td></tr><tr><td>Arrival radius</td><td>0.6 m (waypoint reached)</td></tr></tbody></table><h2 id="pure-fsm-api" tabindex="-1">Pure FSM API <a class="header-anchor" href="#pure-fsm-api" aria-label="Permalink to &quot;Pure FSM API&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { createCaravanFSM, stepCaravanFSM, applyDamageToCaravan } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;../game/ai&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> fsm </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createCaravanFSM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Each fixed step (path is the looping list of XZ waypoints):</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">state</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">moveDX</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">moveDZ</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> stepCaravanFSM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(fsm, caravanPos, playerPos, dt, params, path)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">fsm </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> state</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// When the player&#39;s melee hits:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">fsm </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> applyDamageToCaravan</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(fsm, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">25</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// → flee, or dead at 0 HP</span></span></code></pre></div><h2 id="loot-tables" tabindex="-1">Loot tables <a class="header-anchor" href="#loot-tables" aria-label="Permalink to &quot;Loot tables&quot;">​</a></h2><p>Loot rolls are <strong>deterministic and seeded</strong> — the same seed always yields the same haul, so loot is reproducible and unit-testable.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { rollLoot, DEFAULT_CARAVAN_LOOT } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;../game/loot&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { createSeededRng } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;../game/util&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> drop</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> rollLoot</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">DEFAULT_CARAVAN_LOOT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">createSeededRng</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(seed))</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// drop.items: [{ id, label, qty }, ...] aggregated by item id</span></span></code></pre></div><p>A <code>LootTable</code> is <code>{ rolls, entries }</code>: it performs <code>rolls</code> independent weighted picks, each yielding a quantity in the entry&#39;s <code>[minQty, maxQty]</code>, then aggregates repeats into one stack per id. The default caravan haul:</p><table tabindex="0"><thead><tr><th>Item id</th><th>Label</th><th>Weight</th><th>Qty</th></tr></thead><tbody><tr><td><code>gold</code></td><td>Gold coins</td><td>50</td><td>5–25</td></tr><tr><td><code>grain</code></td><td>Sack of grain</td><td>30</td><td>1–3</td></tr><tr><td><code>cloth</code></td><td>Bolt of cloth</td><td>15</td><td>1–2</td></tr><tr><td><code>blade</code></td><td>Looted blade</td><td>5</td><td>1</td></tr></tbody></table><p>The dropped <code>id</code>s are inventory item ids (see the E3.4 item catalog). The loot event carries only ids + counts; display metadata is resolved from the catalog, keeping the reward payload small.</p><h2 id="reward-event-consumed-by-the-e3-4-inventory" tabindex="-1">Reward event (consumed by the E3.4 inventory) <a class="header-anchor" href="#reward-event-consumed-by-the-e3-4-inventory" aria-label="Permalink to &quot;Reward event (consumed by the E3.4 inventory)&quot;">​</a></h2><p><code>CaravanEnemy</code> implements the <code>Damageable</code> contract from <a href="./melee-combat">E2.2 melee</a>. When the killing blow lands it rolls its table <strong>once</strong> and fires <code>onLooted(drop)</code>. The caller decides what to do with the drop — the caravan stays decoupled from the inventory. The drop is also stashed on <code>caravan.loot</code> for inspection.</p><h2 id="closing-the-loop-caravan-→-inventory-→-hud-e3-5" tabindex="-1">Closing the loop: caravan → inventory → HUD (E3.5) <a class="header-anchor" href="#closing-the-loop-caravan-→-inventory-→-hud-e3-5" aria-label="Permalink to &quot;Closing the loop: caravan → inventory → HUD (E3.5)&quot;">​</a></h2><p>The caravan emits loot and the <a href="./state-management">inventory</a> consumes it, but they speak <strong>different, frozen shapes</strong> on purpose (a schema is forever):</p><table tabindex="0"><thead><tr><th>Side</th><th>Shape</th></tr></thead><tbody><tr><td>Caravan emits (<code>src/game/loot</code>)</td><td><code>LootDrop = { items: LootStack[] }</code>, each <code>{ id, label, qty }</code></td></tr><tr><td>Inventory accepts (<code>pickUpLoot</code>)</td><td><code>{ itemId, count }</code> — one per stack</td></tr></tbody></table><p>A small pure adapter at the integration edge bridges them — <strong><code>src/store/caravanLootAdapter.ts</code></strong> <code>caravanLootToPickups(drop)</code> maps each <code>LootStack</code> to one <code>pickUpLoot</code> payload (<code>id → itemId</code>, <code>qty → count</code>). Neither subsystem&#39;s public shape changes.</p><p>The live wiring runs in the forest zone:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ambush a caravan (forest scene)</span></span>
<span class="line"><span>  → defeat it → CaravanEnemy fires onLooted(drop)</span></span>
<span class="line"><span>  → createForestScene onCaravanLooted callback</span></span>
<span class="line"><span>  → GameCanvas: caravanLootToPickups(drop).forEach(p =&gt; dispatch(pickUpLoot(p)))</span></span>
<span class="line"><span>  → inventory slice accumulates the stacks</span></span>
<span class="line"><span>  → &lt;InventoryPanel&gt; re-renders the new counts in the HUD &quot;Loot&quot; panel</span></span></code></pre></div><p><code>createForestScene</code> and <code>createHumanLandsScene</code> accept an <code>onCaravanLooted?</code> option and spawn caravans into their live zones; the player ambushes them on the same melee path. MPG.5 guarantees the Forest boots with at least 3 caravans and Human Lands boots with at least 2, so the loop is repeatable after a zone enter. <code>GameCanvas</code> supplies the callback only for the live <code>playing</code> scene, so the standalone <code>?dev=caravan</code> playground keeps logging to the console without touching the store.</p><h2 id="ambush-loop-scene-wiring" tabindex="-1">Ambush loop (scene wiring) <a class="header-anchor" href="#ambush-loop-scene-wiring" aria-label="Permalink to &quot;Ambush loop (scene wiring)&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>render loop:</span></span>
<span class="line"><span>  1. stepMeleeAttack — advance player swing state</span></span>
<span class="line"><span>  2. if hitWindowOpen → getMeleeHits([caravan]) → takeDamage on each hit</span></span>
<span class="line"><span>  3. loop.advance → CaravanEnemy.update → stepCaravanFSM (wander/flee)</span></span>
<span class="line"><span>  4. on death → rollLoot → onLooted(drop)</span></span></code></pre></div><h2 id="mesh" tabindex="-1">Mesh <a class="header-anchor" href="#mesh" aria-label="Permalink to &quot;Mesh&quot;">​</a></h2><p><code>CaravanEnemy</code> uses a wide low box as a wagon stand-in (a GLB can replace it later). On defeat the wreck tilts and darkens and stays in place.</p><h2 id="dev-qa-scene" tabindex="-1">Dev / QA scene <a class="header-anchor" href="#dev-qa-scene" aria-label="Permalink to &quot;Dev / QA scene&quot;">​</a></h2><p>Open <strong><code>?dev=caravan</code></strong> (<a href="/korovany/?dev=caravan">caravan playground</a>) for a standalone ambush scene: flat ground, one wandering caravan, full controller + camera + melee. Walk up to it (it flees), strike with <strong>F</strong> until defeated; the rolled loot is logged to the console and, in dev builds, stashed on <code>window.__korovanyCaravanLoot</code>.</p><p>Controls: <strong>WASD</strong> move, <strong>Shift</strong> sprint, <strong>Space</strong> jump, <strong>F</strong> attack, mouse look (click the canvas to capture the pointer).</p><h2 id="tests" tabindex="-1">Tests <a class="header-anchor" href="#tests" aria-label="Permalink to &quot;Tests&quot;">​</a></h2><ul><li><code>src/game/util/seededRandom.test.ts</code> — PRNG determinism, ranges, string seeds.</li><li><code>src/game/loot/lootTable.test.ts</code> — roll determinism, bounds, aggregation, weights, edge cases.</li><li><code>src/game/ai/caravanFSM.test.ts</code> — wander path following, ambush triggers, flee/calm, death + loot.</li><li><code>src/scenes/caravanEnemy.test.ts</code> — Babylon wrapper: wander, flee, one-shot loot emission, determinism.</li><li><code>src/scenes/caravanPlayground.test.ts</code> — full scene wiring boots and emits loot.</li><li><code>src/store/caravanLootAdapter.test.ts</code> — <code>loot → pickUpLoot</code> shape mapping (single, multi-stack, empty) and an end-to-end accumulation into the inventory slice (E3.5).</li><li><code>src/scenes/forestScene.test.ts</code> — the live forest zone spawns a caravan and fires <code>onCaravanLooted</code> exactly once on defeat (E3.5).</li></ul><p>Browser-smoked two ways:</p><ul><li><strong><code>?dev=caravan</code></strong> — the caravan wanders in real WebGL, flees on approach, and emits the loot event (gold/cloth/grain) when defeated.</li><li><strong>Live loot loop (E3.5)</strong> — New Game → defeat the forest caravan → the dropped stacks appear in the HUD <strong>Loot</strong> panel. The dev-only <code>window.__korovanyForestScene</code> handle exposes the live caravan so the join can be driven headlessly.</li></ul>`,37)])])}const g=e(i,[["render",o]]);export{k as __pageData,g as default};
