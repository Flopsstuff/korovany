import{_ as s,o as t,c as a,a2 as o}from"./chunks/framework.BYlK8ftn.js";const g=JSON.parse('{"title":"Forest zone stub","description":"","frontmatter":{},"headers":[],"relativePath":"guide/forest-zone.md","filePath":"guide/forest-zone.md","lastUpdated":1782060627000}'),r={name:"guide/forest-zone.md"};function n(i,e,d,l,c,h){return t(),a("div",null,[...e[0]||(e[0]=[o(`<h1 id="forest-zone-stub" tabindex="-1">Forest zone stub <a class="header-anchor" href="#forest-zone-stub" aria-label="Permalink to &quot;Forest zone stub&quot;">​</a></h1><p>The forest zone (E1.3) is the first playable environment in the Phase-1 vertical slice. It lives in <code>src/scenes/forestScene.ts</code> and wires together the full gameplay spine — streaming assets, third-person controller, follow camera — over a grassy ground plane with a thin-instanced conifer field, streamed huts, and lightweight stump/log/rock/shrub clutter around the spawn clearing.</p><h2 id="try-it" tabindex="-1">Try it <a class="header-anchor" href="#try-it" aria-label="Permalink to &quot;Try it&quot;">​</a></h2><p>Open the app with the <code>?dev=forest</code> flag:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>https://korovany.aimost.pl/?dev=forest   # or http://localhost:5173/?dev=forest</span></span></code></pre></div><p>The scene loads immediately with placeholder boxes for each prop; the real GLBs (forest-tree.glb, wooden-hut.glb) stream in and swap. Controls are identical to the <a href="./character-controller">character controller</a>:</p><table tabindex="0"><thead><tr><th>Action</th><th>Key</th></tr></thead><tbody><tr><td>Move</td><td><strong>W A S D</strong></td></tr><tr><td>Sprint</td><td>hold <strong>Shift</strong></td></tr><tr><td>Jump</td><td><strong>Space</strong></td></tr><tr><td>Look</td><td>mouse (click to lock pointer)</td></tr></tbody></table><h2 id="assets" tabindex="-1">Assets <a class="header-anchor" href="#assets" aria-label="Permalink to &quot;Assets&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Asset</th><th>Registry id</th><th>File</th><th>Tris</th></tr></thead><tbody><tr><td>Conifer tree</td><td><code>env.forest-tree</code></td><td><code>public/models/forest-tree.glb</code></td><td>1357</td></tr><tr><td>Wooden hut</td><td><code>env.wooden-hut</code></td><td><code>public/models/wooden-hut.glb</code></td><td>1893</td></tr><tr><td>Chest</td><td><code>prop.forest-chest</code></td><td><code>public/models/chest.glb</code></td><td>—</td></tr><tr><td>Cargo crate</td><td><code>prop.forest-cargo-crate</code></td><td><code>public/models/cargo-crate.glb</code></td><td>1022</td></tr><tr><td>Caravan wagon</td><td><code>prop.forest-caravan-wagon</code></td><td><code>public/models/caravan-wagon.glb</code></td><td>2827</td></tr><tr><td>Static forest elf</td><td><code>npc.forest-static-elf</code></td><td><code>public/models/korovany_hero_player-default.glb</code></td><td>2884</td></tr></tbody></table><p>The tree and hut were generated in <a href="/korovany/FLO/issues/FLO-299">FLO-299</a> by Pygmalion using the Meshy pipeline under visual-language v1.2 (≤ 3000 tris). FLO-470 adds the leftover cargo crate, wagon, and two static elf placements; FLO-473 promotes <code>chest.glb</code> from one-off decor to four healing chest placements. Register them via <code>seedForestAssets(registry)</code> from <code>src/scenes/forestScene.ts</code>; streamed GLBs are normalized and passed through the shared matte/faceted <code>flatShade()</code> conform before placement.</p><h2 id="how-it-works" tabindex="-1">How it works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How it works&quot;">​</a></h2><p><code>createForestScene(canvas, options)</code> boots a Babylon <code>Scene</code> and:</p><ol><li><strong>Ground</strong> — built by <code>createWorldBounds</code> (<code>worldBounds.ts</code>): a 600 × 600 unit <code>MeshBuilder.CreateGround</code> with <code>isPickable: true</code> so the controller&#39;s downward ray lands correctly. The plane stays perfectly flat (ray-safe) but is subdivided (<code>subdivisions: 48</code>), flat-shaded, and given a deterministic per-facet brightness jitter via vertex colours, on a matte (near-zero specular) muted-olive <code>Color3(0.27, 0.36, 0.21)</code> material — so it reads as a low-poly faceted surface, not a &quot;plain green gradient&quot; (P7.4 / FLO-422). Desaturated from the old pure grass green so the ground recedes and props/enemies read as the foreground.</li><li><strong>Streaming</strong> — <code>AssetRegistry</code> + <code>AssetStreamLoader</code> (same system as E1.2). <code>seedForestAssets</code> registers the tree and hut URLs. Each prop calls <code>spawnStreamedInstance</code> which shows a placeholder box immediately and swaps to the GLB on load.</li><li><strong>Props</strong> — 3 streamed huts, 4 healing chests, and the leftover cargo/wagon/elf decor placed at hard-coded (x, z) coordinates, plus cheap procedural stumps, logs, rocks, and shrubs around the spawn clearing. The inner 3.5-unit radius stays clear so the first combat beats have readable movement space. The conifers themselves are now a thin-instanced field (see <strong>Conifer field</strong> below), not streamed placements.</li><li><strong>Healing chests</strong> — <code>FOREST_HEALING_CHEST_SPECS</code> mirrors the streamed chest placements. Standing within 2.25 m of a chest restores 5 HP every 0.5 s via <code>onPlayerHealed</code>; chests are reusable cooldown stations, not consumed pickups.</li><li><strong>Controller + camera</strong> — the same <code>CharacterController</code> and <code>ThirdPersonCamera</code> from E1.1, spawned at <code>(0, 2, 0)</code> above the ground.</li></ol><h2 id="conifer-field-flo-482" tabindex="-1">Conifer field (FLO-482) <a class="header-anchor" href="#conifer-field-flo-482" aria-label="Permalink to &quot;Conifer field (FLO-482)&quot;">​</a></h2><p>The forest&#39;s trees are a <strong>thin-instanced conifer field</strong>, not streamed placements. Two reasons drove the change:</p><ul><li><strong>The streamed trees collapsed to one.</strong> The <code>AssetStreamLoader</code> ref-counts a single cached model per asset id, so every <code>env.forest-tree</code> placement in the zone manifest shared the same root — the old &quot;12 trees&quot; only ever rendered as one. The tree placements were removed from <code>FOREST_MANIFEST</code>; <code>forest-tree.glb</code> is now loaded directly by the field.</li><li><strong>The GLB ships white.</strong> <code>forest-tree.glb</code> is a single mesh with no material and no textures, so it rendered white — which the board flagged as wrong.</li></ul><p><code>scenes/forestTrees.ts</code> owns the field:</p><ul><li><code>mountForestTreeField(scene, options)</code> — fire-and-forget (mirrors <code>survivorAvatar</code>): loads the GLB once, then builds the field. A failed fetch (headless tests) is swallowed; <code>forestTreeLoader: null</code> on <code>createForestScene</code> skips it entirely.</li><li><code>applyForestTreeFoliage</code> — hard-facets the mesh (<code>convertToFlatShadedMesh</code>, v1.2 read) and paints it a <strong>flat matte green</strong> (<code>Color3(0.13, 0.4, 0.16)</code>, near-zero specular, <strong>no texture</strong>). This is what stops the trees rendering white. The GLB is a single mesh, so there is no separate trunk submesh to colour — the whole conifer is green.</li><li><code>buildForestTreeField</code> → <code>createInstancedVegetation</code> — thin-instances the greened prototype across the scatter. The whole field costs <strong>one draw call per submesh regardless of tree count</strong>, so the higher count carries no perf regression.</li></ul><p>The scatter is a pure, seeded generator — <code>generateForestTreePlacements</code> in <code>game/streaming/forestTreeField.ts</code>:</p><ul><li><strong>Count</strong> — <code>FOREST_TREE_FIELD_COUNT</code> (64), comfortably more than the old dozen.</li><li><strong>Layout</strong> — a non-grid ring from <code>FOREST_TREE_FIELD_CLEARING_RADIUS</code> (7, spawn stays clear) to <code>FOREST_TREE_FIELD_OUTER_RADIUS</code> (58), <code>sqrt</code>-weighted so density is even across the disc.</li><li><strong>Size</strong> — each tree gets a uniform random scale in <strong>×1…×3</strong> (<code>FOREST_TREE_FIELD_MIN_SCALE</code>…<code>FOREST_TREE_FIELD_MAX_SCALE</code>) of the ×1 authored size (<code>FOREST_TREE_BASE_SIZE</code>, 4 units), plus a random yaw.</li><li><strong>Deterministic</strong> — seeded by the fixed <code>FOREST_TREE_FIELD_SEED</code> (never <code>Date.now()</code>), so the forest is reproducible and the Vitest scatter/scale/ material assertions never flake.</li></ul><h2 id="avatar-ground-feel" tabindex="-1">Avatar &amp; ground feel <a class="header-anchor" href="#avatar-ground-feel" aria-label="Permalink to &quot;Avatar &amp; ground feel&quot;">​</a></h2><p>The player is rendered by the <strong>flat-albedo survivor GLB</strong> (<code>korovany_hero_player-default.glb</code>, 2,884 tris · 1× 1024 flat base-color), mounted by <code>mountSurvivorAvatar</code> (<code>src/scenes/survivorAvatar.ts</code>) and <strong>faceted in-engine</strong> (FLO-443). The mount is <strong>fire-and-forget</strong>, mirroring <code>soldierEnemy</code>/<code>archerEnemy</code>: the scene factory stays synchronous, gameplay runs on the invisible capsule from frame 0, and the visual mesh pops in (~200 ms) once the GLB resolves. At load each geometry mesh gets <code>convertToFlatShadedMesh()</code> (so the silhouette reads hard-faceted, not smooth — the v1.2 low-poly read) plus a matte near-zero-specular material; the welded <code>root</code> is parented to the capsule at <code>(0, -0.9, 0)</code> so the feet sit on the ground plane (the &quot;ground feel&quot;), every part is <code>isPickable: false</code> so it never intercepts the controller&#39;s downward ray, and the <code>root</code> is handed to <code>controller.animator.node</code> for procedural motion (bob/lean/lunge/topple). <code>heroUrl: null</code> skips the avatar for headless tests; any other value (including the default GLB path) mounts the survivor.</p><p><strong>Rig-less, animated procedurally (FLO-440 → FLO-443).</strong> The GLB is a single welded mesh with <strong>no skeleton/bones</strong>, re-textured in FLO-440 to a flat, matte, unlit base-color (v1.2-compliant) and posed in a natural relaxed survivor idle. bob/lean/lunge/topple are whole-body <code>root</code> transforms, so the rig-less single mesh satisfies the animator contract unchanged. The capsule is the placeholder if the fetch ever fails; a cheap placeholder box could be added later if the brief pop-in matters — it currently does not.</p><h2 id="scene-options" tabindex="-1">Scene options <a class="header-anchor" href="#scene-options" aria-label="Permalink to &quot;Scene options&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ForestSceneOptions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  createEngine</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">canvas</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> HTMLCanvasElement</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AbstractEngine</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // for tests</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  heroUrl</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                       // hero GLB path</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  onPlayerDamaged</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">amount</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> void</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                    // enemy hit → dispatch</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  corpseStore</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CorpseStore</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                     // E2.4 persistence</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  corpseGlbUrl</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                  // corpse GLB path</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  isPaused</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                      // pause gate (FLO-326)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Pass <code>heroUrl: null</code> / <code>corpseGlbUrl: null</code> and inject a <code>NullEngine</code> in tests to skip network fetches.</p><p>The returned <code>ForestScene</code> exposes <code>step(dt)</code> — the exact per-frame function the render loop runs — so tests can advance the simulation deterministically instead of relying on <code>requestAnimationFrame</code>.</p><h2 id="pause-gating" tabindex="-1">Pause gating <a class="header-anchor" href="#pause-gating" aria-label="Permalink to &quot;Pause gating&quot;">​</a></h2><p>While the game is <strong>paused</strong> (ESC), the whole forest simulation freezes — soldier AI, player movement, and melee damage do not advance — so the player <strong>cannot take damage or die on the pause screen</strong> (FLO-326). The scene still renders the frozen frame so it stays visible under the React pause overlay.</p><p>The gate is the <code>app.phase</code> state machine, the single source of truth for &quot;is the sim live&quot;. <code>GameCanvas</code> passes <code>isPaused: () =&gt; phase === &#39;paused&#39;</code> (read live via a ref so a pause toggle never remounts the scene), and <code>step()</code> early-returns after <code>scene.render()</code> whenever it reports <code>true</code>. Death is likewise only processed while <code>phase === &#39;playing&#39;</code> (see <a href="./state-management">state-management.md</a>), never while paused.</p><h2 id="enemies-corpses" tabindex="-1">Enemies &amp; corpses <a class="header-anchor" href="#enemies-corpses" aria-label="Permalink to &quot;Enemies &amp; corpses&quot;">​</a></h2><p>The forest spawns at least five Empire soldier patrols (MPG.5, see <a href="./enemy-ai">enemy-ai.md</a>) plus two ranged Empire archers (FLO-432), placed from the zone-content <code>encounterAnchors</code> (<code>src/game/world/zoneContent.ts</code>, FLO-411). The archers are seeded <strong>behind the melee wave</strong> (~34 m out, past every soldier), so the player meets a melee front before any arrows fly. When a soldier or archer dies the scene converts it into a persistent, inert corpse via <code>reapDeadEnemies</code> + <code>CorpseManager</code> (the archer falls as a ranger, its own GLB). Corpses are capped and survive a zone re-enter within a session — full details in <a href="./corpses">corpses.md</a>.</p><h2 id="safe-spawn-difficulty-curve-p7-1-flo-412" tabindex="-1">Safe spawn &amp; difficulty curve (P7.1 / FLO-412) <a class="header-anchor" href="#safe-spawn-difficulty-curve-p7-1-flo-412" aria-label="Permalink to &quot;Safe spawn &amp; difficulty curve (P7.1 / FLO-412)&quot;">​</a></h2><p>A live audit of the deployed build found the forest first session <strong>unwinnable</strong>: the player spawned at the origin with a soldier seeded ~8.5 m away — <em>inside</em> its 10 m aggro radius — so two patrols converged instantly (~20 HP/s) and killed a fresh player in ~15 s, before they reached any caravan. The first session is now made survivable <strong>by placement first, lethality second</strong>:</p><ul><li><p><strong>Soldier-free spawn buffer.</strong> No soldier anchor sits within <code>SAFE_SPAWN_BUFFER</code> (18 m, comfortably above the 10 m <code>detectionRadius</code>) of the player spawn. An idle player at the origin is never aggroed, and the nearest caravan (<code>caravan-1</code>, 10 m) sits inside the buffer for a free first raid.</p></li><li><p><strong>Ramped encounter.</strong> The five soldier anchors are distributed by distance: one lone first encounter just past the buffer (19 m, dead ahead), the other four clustered in pairs guarding the two far caravans (24–26 m). The player meets <strong>one</strong> soldier first, not a wall of five on spawn. Total counts are unchanged from MPG.5 — only the distribution. The anchors live in <code>zoneContent.ts</code> (the single source of truth) so the curve is data, not magic numbers in the scene.</p></li><li><p><strong>Spawn grace.</strong> As a lethality backstop, soldier hits deal <strong>zero damage</strong> for the first <code>SPAWN_GRACE_SECONDS</code> (2 s) of live play (<code>spawnGraceDamageScale</code>), so a player who walks straight into a patrol gets a beat to react instead of being deleted on contact. The grace clock advances only while unpaused.</p></li><li><p><strong>Patrol leash.</strong> The buffer above is a <em>placement</em> guarantee — but soldiers patrol. Each soldier is leashed to within <code>patrolLeashRadius</code> (6 m) of its spawn anchor (<code>soldierFSM</code>, FLO-412), so the lone 19 m soldier can approach the origin no closer than 13 m — still outside its 10 m <code>detectionRadius</code>. Without the leash a patrol drifts unbounded and re-enters the cleared spawn within ~30 s, which is exactly the regression the survival guard test now catches.</p></li></ul><p>The player walks at 4 m/s versus the soldier chase speed of 3 m/s, so a careful player can kite a cluster and pull patrols one at a time — challenge is retained, not removed. Global <code>DEFAULT_SOLDIER_PARAMS</code> are untouched (the human-lands zone keeps its tuning); all changes are local to the forest zone.</p><h2 id="tests" tabindex="-1">Tests <a class="header-anchor" href="#tests" aria-label="Permalink to &quot;Tests&quot;">​</a></h2><ul><li><code>forestScene.test.ts</code> — <code>seedForestAssets</code> (registry entries + sizes), spawn clutter placement, scene boot (live camera, capsule, ground pickable), dispose idempotency, corpse re-spawn on boot, the <code>reapDeadSoldiers</code> live→corpse transition, the <strong>pause-gating</strong> regression (FLO-326): stepping the scene while paused moves no soldier and deals no player damage, with a live control proving combat does advance when unpaused, and the <strong>safe-spawn &amp; difficulty curve</strong> guards (FLO-412): no soldier within <code>SAFE_SPAWN_BUFFER</code> of the spawn, exactly one first-encounter soldier, and the spawn grace nullifying then restoring damage.</li></ul>`,38)])])}const u=s(r,[["render",n]]);export{g as __pageData,u as default};
