import{_ as a,o as t,c as s,a2 as i}from"./chunks/framework.BYlK8ftn.js";const m=JSON.parse('{"title":"Enemy AI — Empire Soldier (E2.3)","description":"","frontmatter":{},"headers":[],"relativePath":"guide/enemy-ai.md","filePath":"guide/enemy-ai.md","lastUpdated":1782025194000}'),n={name:"guide/enemy-ai.md"};function d(r,e,o,l,h,p){return t(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="enemy-ai-—-empire-soldier-e2-3" tabindex="-1">Enemy AI — Empire Soldier (E2.3) <a class="header-anchor" href="#enemy-ai-—-empire-soldier-e2-3" aria-label="Permalink to &quot;Enemy AI — Empire Soldier (E2.3)&quot;">​</a></h1><p>The first enemy archetype is an Empire soldier with a deterministic finite-state machine (FSM). Pure game logic lives in <code>src/game/ai/</code>; the Babylon mesh wrapper in <code>src/scenes/soldierEnemy.ts</code>.</p><h2 id="behaviour-states" tabindex="-1">Behaviour states <a class="header-anchor" href="#behaviour-states" aria-label="Permalink to &quot;Behaviour states&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>patrol ──[player inside detectionRadius]──▶ chase</span></span>
<span class="line"><span>chase  ──[player inside attackRadius]────▶ attack</span></span>
<span class="line"><span>chase  ──[player escapes 1.3× detection]──▶ patrol</span></span>
<span class="line"><span>attack ──[player outside 1.5× attackRadius]▶ chase</span></span>
<span class="line"><span>order  ──[follow/hold/move-to/attack-target]▶ ordered behaviour</span></span>
<span class="line"><span>order cleared ─────────────────────────────▶ patrol</span></span>
<span class="line"><span>any    ──[HP == 0]───────────────────────▶ dead  (terminal)</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Phase</th><th>Behaviour</th></tr></thead><tbody><tr><td><strong>patrol</strong></td><td>Wander at 1.5 m/s; change direction every 3 s</td></tr><tr><td><strong>chase</strong></td><td>Move toward player at 3 m/s</td></tr><tr><td><strong>attack</strong></td><td>Deal 15 HP every 1.5 s to the player</td></tr><tr><td><strong>follow</strong></td><td>Follow a commander until inside the follow distance</td></tr><tr><td><strong>hold</strong></td><td>Hold position until a new order arrives or the order is cleared</td></tr><tr><td><strong>move-to</strong></td><td>Move to an ordered destination, then hold</td></tr><tr><td><strong>attack-target</strong></td><td>Attack the ordered hostile target; if it disappears or dies, hold</td></tr><tr><td><strong>dead</strong></td><td>Stop all behaviour; the scene converts it to a persistent corpse — see <a href="./corpses">corpses.md</a> (E2.4)</td></tr></tbody></table><h2 id="default-parameters" tabindex="-1">Default parameters <a class="header-anchor" href="#default-parameters" aria-label="Permalink to &quot;Default parameters&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Max HP</td><td>60</td></tr><tr><td>Detection radius</td><td>10 m</td></tr><tr><td>Attack radius</td><td>1.8 m</td></tr><tr><td>Attack damage</td><td>15 HP</td></tr><tr><td>Attack cooldown</td><td>1.5 s</td></tr><tr><td>Patrol speed</td><td>1.5 m/s</td></tr><tr><td>Chase speed</td><td>3.0 m/s</td></tr><tr><td>Follow distance</td><td>2.5 m</td></tr><tr><td>Move-to arrival radius</td><td>0.35 m</td></tr></tbody></table><h2 id="pure-fsm-api" tabindex="-1">Pure FSM API <a class="header-anchor" href="#pure-fsm-api" aria-label="Permalink to &quot;Pure FSM API&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { createSoldierFSM, stepSoldierFSM, applyDamageToSoldier } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;../game/ai&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> fsm </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createSoldierFSM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Each fixed step:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">state</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">moveDX</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">moveDZ</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">attacked</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> stepSoldierFSM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(fsm, soldierPos, playerPos, dt)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">fsm </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> state</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// When player melee hits:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">fsm </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> applyDamageToSoldier</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(fsm, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">25</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><h2 id="commander-orders-e4-3" tabindex="-1">Commander orders (E4.3) <a class="header-anchor" href="#commander-orders-e4-3" aria-label="Permalink to &quot;Commander orders (E4.3)&quot;">​</a></h2><p>Pure command intake lives in <code>src/game/ai/orders.ts</code>. A commander issues one of four order types to same-faction subordinates:</p><table tabindex="0"><thead><tr><th>Order</th><th>Intake validation</th><th>Soldier behaviour</th></tr></thead><tbody><tr><td><code>follow</code></td><td>Recipient must be alive, same faction, and assigned to the commander</td><td>Move toward the commander until inside <code>followDistance</code></td></tr><tr><td><code>hold</code></td><td>Same recipient checks</td><td>Stop moving and wait</td></tr><tr><td><code>move-to</code></td><td>Same recipient checks; destination must be finite</td><td>Move to the destination, then transition to <code>hold</code></td></tr><tr><td><code>attack-target</code></td><td>Same recipient checks; target must exist, be alive, and be hostile by <code>resolveStance</code></td><td>Move into attack radius, deal ordered-target damage, then hold if the target is gone</td></tr></tbody></table><p>The boundary is deliberate: <code>issueSquadOrder()</code> rejects invalid recipients, destinations, and non-hostile targets up front. <code>SoldierEnemy</code> and <code>stepSoldierFSM()</code> then consume a compact <code>SoldierOrderContext</code> and trust it, instead of re-running faction checks every frame. When an order is cleared, an ordered soldier resumes default patrol/aggro behaviour.</p><p><code>?dev=orders</code> opens a standalone commander playground. It scripts the same intake path through <code>follow</code>, <code>move-to</code>, and <code>attack-target</code> orders against a hostile target, without changing default forest gameplay.</p><h2 id="damageable-contract" tabindex="-1">Damageable contract <a class="header-anchor" href="#damageable-contract" aria-label="Permalink to &quot;Damageable contract&quot;">​</a></h2><p><code>SoldierEnemy</code> implements the <code>Damageable</code> interface from E2.2 (melee combat). The player&#39;s <code>getMeleeHits</code> call returns living <code>SoldierEnemy</code> instances; the caller invokes <code>takeDamage(amount)</code> on each.</p><h2 id="fight-loop-forestscene-wiring" tabindex="-1">Fight loop (ForestScene wiring) <a class="header-anchor" href="#fight-loop-forestscene-wiring" aria-label="Permalink to &quot;Fight loop (ForestScene wiring)&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>render loop:</span></span>
<span class="line"><span>  1. stepMeleeAttack — advance player swing state</span></span>
<span class="line"><span>  2. if hitWindowOpen → getMeleeHits → takeDamage on each hit soldier</span></span>
<span class="line"><span>  3. loop.advance → runs SoldierEnemy.update → stepSoldierFSM</span></span>
<span class="line"><span>  4. if attacked → onPlayerDamaged(15) → dispatch(damagePlayer(15))</span></span>
<span class="line"><span>  5. if player HP == 0 → App.tsx death handler → returnToMenu</span></span></code></pre></div><h2 id="mesh" tabindex="-1">Mesh <a class="header-anchor" href="#mesh" aria-label="Permalink to &quot;Mesh&quot;">​</a></h2><p><code>SoldierEnemy</code> carries an invisible capsule as its position/collision proxy and mounts the <strong>FLO-311 Empire soldier GLB</strong> (<code>/models/empire-soldier.glb</code>) onto it, mirroring how the hero model is parented to the player capsule. The GLB load is best-effort: if the model can&#39;t be fetched (e.g. headless tests), the bare capsule placeholder stays visible. The FLO-311 GLB is a preview mesh with no baked texture pass, so <code>applyEmpireSoldierTexture()</code> assigns an in-engine Empire palette (green coat, brown leather, muted metal) to loaded submeshes and the placeholder capsule uses the same coat tone. Pass <code>glbUrl: null</code> to force the placeholder. On death the mesh topples procedurally and stays in place for E2.4 to take over.</p><h2 id="spawn" tabindex="-1">Spawn <a class="header-anchor" href="#spawn" aria-label="Permalink to &quot;Spawn&quot;">​</a></h2><p>Zone scenes spawn soldiers from local placement tables and register each <code>SoldierEnemy</code> with the fixed-step scheduler. MPG.5 guarantees the Forest boots with at least 5 soldier patrols and Human Lands boots with at least 3; Y is the capsule half-height (<code>0.9</code>) so each patrol rests on the ground.</p><h2 id="tests" tabindex="-1">Tests <a class="header-anchor" href="#tests" aria-label="Permalink to &quot;Tests&quot;">​</a></h2><p><code>src/game/ai/orders.test.ts</code> covers command intake validation. <code>src/game/ai/soldierFSM.test.ts</code> covers default FSM transitions plus ordered follow/hold/move/attack behaviours. <code>src/scenes/ordersPlayground.test.ts</code> smoke-tests the dev scene wiring, and <code>src/scenes/soldierEnemy.test.ts</code> covers the scene wrapper plus the in-engine soldier material palette.</p>`,24)])])}const k=a(n,[["render",d]]);export{m as __pageData,k as default};
