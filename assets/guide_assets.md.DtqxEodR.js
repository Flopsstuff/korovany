import{_ as t,o,c as a,a2 as r}from"./chunks/framework.BYlK8ftn.js";const p=JSON.parse('{"title":"Generated 3D assets","description":"","frontmatter":{},"headers":[],"relativePath":"guide/assets.md","filePath":"guide/assets.md","lastUpdated":1782058331000}'),s={name:"guide/assets.md"};function d(n,e,i,l,c,h){return o(),a("div",null,[...e[0]||(e[0]=[r('<h1 id="generated-3d-assets" tabindex="-1">Generated 3D assets <a class="header-anchor" href="#generated-3d-assets" aria-label="Permalink to &quot;Generated 3D assets&quot;">​</a></h1><p>Catalog of the game&#39;s generated 3D models. All models are produced <strong>per concrete character/prop ticket only</strong> (never speculatively — board directive <a href="/korovany/plan/game-plan">FLO-270</a>) via the <code>tools/meshy-3d</code> pipeline, in <strong>low-poly visual language v1.2</strong> (≤ 3000 tris/object, flat-shaded palette materials).</p><p>Files live under <code>public/models/*.glb</code> and stream into the scene through the <a href="./asset-streaming">asset-streaming</a> registry. Binaries are stored in <strong>Git LFS</strong> (see <code>.gitattributes</code>). Generation is reproducible from the recorded Meshy task id (<code>python tools/meshy-3d/meshy.py status &lt;task_id&gt; --kind text-to-3d</code>).</p><blockquote><p><strong>Audio:</strong> this catalog covers <strong>binary visual assets only</strong>. The game ships <strong>no audio files</strong> — sound effects are synthesized procedurally at runtime (raw Web Audio), so there is nothing to license or store in LFS. See <a href="./audio"><code>audio.md</code></a> for the SFX system and the synthesis rationale.</p></blockquote><h2 id="licensing" tabindex="-1">Licensing <a class="header-anchor" href="#licensing" aria-label="Permalink to &quot;Licensing&quot;">​</a></h2><p>Generated assets are cleared for FlopBut&#39;s intended (incl. commercial) use under the Meshy paid-plan terms, which grant full commercial ownership of generated output. No infringing reference inputs are used, and these assets are kept out of any Community/showcase page.</p><h2 id="characters-props" tabindex="-1">Characters &amp; props <a class="header-anchor" href="#characters-props" aria-label="Permalink to &quot;Characters &amp; props&quot;">​</a></h2><blockquote><p><strong>Player avatar — flat-albedo survivor GLB, faceted in-engine, live (FLO-440 → FLO-443).</strong> The live player visual is the <code>korovany_hero_player-default.glb</code> survivor avatar (2,884 tris · 1× 1024 flat base-color), mounted by <code>mountSurvivorAvatar</code> (<code>src/scenes/survivorAvatar.ts</code>) across the forest, human-lands, and controller-playground scenes. An earlier interim used a procedural box-fighter (<code>playerAvatar.ts</code>, FLO-422) while the GLB carried two defects — a wide arms-out &quot;scarecrow&quot; T-pose and a semi-realistic surface; <strong>both are fixed in the GLB</strong>: FLO-434 reposed it to a natural arms-down survivor idle, FLO-440 re-textured it to a flat, matte, unlit base-color (v1.2-compliant — see hero README v13), and FLO-443 wired it in (retiring the box-fighter). The GLB is <strong>rig-less</strong>: the mount applies <code>mesh.convertToFlatShadedMesh()</code> + a matte material at load (as <code>worldBounds.ts</code> does for the ground) so the faceted hard-edge read lands, and whole-body bob/lean/lunge/topple drive the single <code>root</code> via the animator contract.</p><p><strong>Combat-state swap (FLO-481 / plan step 2 of FLO-474).</strong> The player now has two distinct visual states. Alongside the neutral <code>…-default.glb</code>, combat scenes mount <code>korovany_hero_player-attack.glb</code> — a drop-in strike-pose twin (2,905 tris, same bbox/scale/identity, FLO-480) — and toggle which one renders by combat state: <strong>default while idle, attack while a melee swing is in flight</strong> (the windup/active/recovery phases). <code>mountSurvivorAvatar(scene, mount, heroUrl, attackUrl)</code> loads both and parents them under the capsule at the same foot offset; <code>CharacterController.setAttackPhase</code> edge-fires a registered swap on the idle↔swing transition (so it flips twice per swing, not every frame). Because the two GLBs share a bbox the swap needs no scale/position correction, and there is no animation playback — the static loader simply switches the visible mesh (FLO-440 swap-by-URL pattern). Live in forest, human-lands, mountains, and empire (every scene with melee); the controller-playground (no combat) stays default-only.</p></blockquote><blockquote><p><strong>Art coherence — every character is faceted in-engine (FLO-452).</strong> The player is not the only character that gets the flat-shaded treatment. The shipped enemy/hero GLBs are smooth, semi-glossy meshes, so the same in-engine facet+matte conform is applied at GLB-load time to <strong>every</strong> character so they all read in one v1.2 low-poly band: the <strong>enemy soldier</strong> (<code>soldierEnemy.ts</code>), the <strong>corpse body</strong> (<code>corpseManager.ts</code>), and the <strong>menu / won / lost &quot;defeat screen&quot; backdrop hero</strong> (<code>engine/index.ts</code>, streamed via <code>createGameEngine</code>). The treatment is single-sourced in <code>src/game/util/flatShade.ts</code> (<code>facetMeshes</code> / <code>mattenMaterial</code> / <code>flatShade</code>), which <code>survivorAvatar.ts</code> also uses — so all characters share exactly one definition of &quot;flat low-poly.&quot; Note this supersedes the interim procedural-box approach once explored for FLO-452: with the player now a faceted GLB (FLO-447), box enemies would <em>break</em> coherence, not create it; matching the player&#39;s faceted-GLB read is the correct conform. As with the player, the faceting only manifests at runtime, so the visual-truth gate is an in-scene screenshot, never code inspection.</p></blockquote><table tabindex="0"><thead><tr><th>Asset</th><th>File</th><th>Tris</th><th>Size</th><th>Rig</th><th>Meshy task id</th></tr></thead><tbody><tr><td><strong>Player hero (survivor) — neutral/idle</strong> <em>(v1.2-compliant; live)</em></td><td><code>public/models/korovany_hero_player-default.glb</code></td><td>2884</td><td>298 KiB</td><td>static (no skeleton); arms-down idle (FLO-434) + flat-albedo re-author (FLO-440, v13)</td><td><code>019ee92c-e565-7b9c-b7e5-cad74053e786</code> (geometry preview) / <code>019ee94d-b328-7308-a8fa-d5274bcc1a99</code> (flat retexture)</td></tr><tr><td><strong>Player hero (survivor) — attack pose</strong> <em>(v1.2-compliant; live, swapped by combat state FLO-481)</em></td><td><code>public/models/korovany_hero_player-attack.glb</code></td><td>2905</td><td>295 KiB</td><td>static (no skeleton); forward strike, drop-in twin of default (FLO-480)</td><td>—</td></tr><tr><td>Conifer tree</td><td><code>public/models/forest-tree.glb</code></td><td>1357</td><td>—</td><td>static</td><td>—</td></tr><tr><td>Wooden hut</td><td><code>public/models/wooden-hut.glb</code></td><td>1893</td><td>—</td><td>static</td><td>—</td></tr><tr><td>Chest <em>(wired: forest static loot decor, FLO-470)</em></td><td><code>public/models/chest.glb</code></td><td>—</td><td>—</td><td>static</td><td>—</td></tr><tr><td><strong>Empire soldier (enemy)</strong></td><td><code>public/models/empire-soldier.glb</code></td><td><strong>2794</strong></td><td>130 KiB</td><td>static (no skeleton)</td><td><code>019ee601-93f0-7988-86f8-e35ce1067881</code></td></tr><tr><td><strong>Roadside shrine (Salt Road)</strong></td><td><code>public/models/roadside-shrine.glb</code></td><td><strong>1984</strong></td><td>394 KiB</td><td>static</td><td><code>019ee73a-c8a6-73bd-a2ec-e21137a6dba2</code> (preview) / <code>019ee747-ae35-786a-a8c7-490e65d0cddd</code> (retexture)</td></tr><tr><td><strong>Empire toll gate</strong> <em>(wired: human-lands landmark, FLO-478)</em></td><td><code>public/models/toll-gate.glb</code></td><td><strong>1947</strong></td><td>378 KiB</td><td>static</td><td>preview <code>019ee748-205e-763a-a883-bdda11e91c7e</code> · retexture <code>019ee749-ae59-7680-aa94-e6a8842c7bd5</code></td></tr><tr><td><strong>Caravan wagon (Salt Road)</strong> <em>(wired: caravan enemy visual + forest static prop, FLO-470)</em></td><td><code>public/models/caravan-wagon.glb</code></td><td><strong>2827</strong></td><td>394 KiB</td><td>static</td><td><code>019ee749-d4cf-79b9-b813-d98be2201197</code> (preview) / <code>019ee74d-2300-772c-8a22-612c27dd99dc</code> (retexture)</td></tr><tr><td><strong>Cargo crate (Salt Road)</strong> <em>(wired: forest cargo decor, FLO-470)</em></td><td><code>public/models/cargo-crate.glb</code></td><td><strong>1022</strong></td><td>296 KiB</td><td>static</td><td><code>019ee743-a02c-7878-a22e-7b67cdfbafa6</code> (preview)</td></tr><tr><td><strong>Ruined watchtower (Salt Road)</strong> <em>(wired: forest landmark, FLO-476)</em></td><td><code>public/models/watchtower.glb</code></td><td><strong>2564</strong></td><td>379 KiB</td><td>static</td><td><code>019ee749-6051-7990-b286-98be4ecf82b4</code> (retexture)</td></tr><tr><td><strong>Ranged enemy (archer)</strong></td><td><code>public/models/ranged-archer.glb</code></td><td><strong>2589</strong></td><td>258 KiB</td><td>static (no skeleton)</td><td>preview <code>019ee91a-3128-705b-8acc-3d10e03e9930</code> · retexture <code>019ee91c-7315-78b1-8841-a90f77930d17</code></td></tr><tr><td><strong>Empire palace-guard</strong></td><td><code>public/models/empire-palace-guard.glb</code></td><td><strong>2793</strong></td><td>357 KiB</td><td>static (no skeleton)</td><td>retexture <code>019ee91c-7f15-78b4-9665-5f7c18268eec</code> (of soldier <code>019ee601-93f0-7988-86f8-e35ce1067881</code>)</td></tr></tbody></table><h3 id="empire-toll-gate-—-phase-3-5-salt-road-pack" tabindex="-1">Empire toll gate — Phase 3.5 (Salt Road pack) <a class="header-anchor" href="#empire-toll-gate-—-phase-3-5-salt-road-pack" aria-label="Permalink to &quot;Empire toll gate — Phase 3.5 (Salt Road pack)&quot;">​</a></h3><p>Salt Road landmark and navigation anchor (world-specs.md §1). Generated for <a href="/korovany/FLO/issues/FLO-373">FLO-373</a>.</p><ul><li><strong>Budget:</strong> 1947 tris ≤ 2500 (v1.2). Bounding box ≈ 2.0 × 1.6 × 0.7 units (road-spanning prop). Textured (retexture pass). Web payload <strong>378 KiB</strong> — in line with the other Salt Road props (296–394 KiB).</li><li><strong>Provenance:</strong> Meshy text-to-3D preview at <code>--target-polycount 2000 --art-style realistic</code>, followed by retexture pass. Prompt: <em>&quot;low-poly stylized empire toll gate, two wooden barrier arms across a road, small wooden ledger booth, tall flagpole with imperial banner, weathered wood and iron, faceted flat-shaded, austere straight imperial design, readable silhouette&quot;</em>. Retexture style: <em>&quot;weathered wood planks, rusted iron fittings, imperial red and gold banner, stone road surface, earthy tones, flat low-poly colors&quot;</em>. 20 preview + 10 retexture = 30 Meshy credits.</li><li><strong>Texture budget (FLO-477):</strong> Meshy retexture embedded a single 2048² PBR JPEG (~4.0 MiB), pushing the GLB to 4.1 MiB — ~10× over the Salt Road web budget on a 1947-tri mesh. Downscaled to <strong>1024²</strong> (q85) via <code>tools/meshy-3d/resize_glb_textures.py</code>, recompacting the BIN chunk; mesh/accessor data is byte-identical, so the faceted/matte silhouette is untouched (texture 4015 KiB → 263 KiB; GLB 4130 KiB → 378 KiB). No Meshy credits spent (purely local repack).</li><li><strong>Rig:</strong> static mesh, no skeleton.</li><li><strong>Verification:</strong> loads headless via <code>node tools/meshy-3d/smoke_load_glb.mjs public/models/toll-gate.glb</code> → 2 meshes, 1947 tris, no errors. Resized texture atlas inspected: empire palette (grey planks, red/gold banner) and matte stylized read preserved.</li><li><strong>Wired (FLO-478):</strong> <code>spawnTollGateProp</code> in <code>src/scenes/tollGateProp.ts</code> mounts the GLB at the <code>toll-gate</code> landmark position from <code>zoneContent</code>, applies the shared <code>flatShade()</code> matte/faceted conform, and removes the procedural greybox placeholder from <code>humanLandsScene.ts</code>.</li></ul><h3 id="ruined-watchtower-salt-road-landmark-—-phase-3-5-mpg" tabindex="-1">Ruined watchtower (Salt Road landmark) — Phase 3.5 / MPG <a class="header-anchor" href="#ruined-watchtower-salt-road-landmark-—-phase-3-5-mpg" aria-label="Permalink to &quot;Ruined watchtower (Salt Road landmark) — Phase 3.5 / MPG&quot;">​</a></h3><p>Tall round ruined stone tower, 3–4 stories, two crenellated tiers, a doorway at the base and a rubble skirt — a strong vertical silhouette to serve as a navigation anchor on the Velya Salt Road (<a href="./worlds/velya-salt-road">world-specs</a>, <code>world-specs.md</code> landmark list). Generated for <a href="/korovany/plan/game-plan">FLO-374</a>; replaces the placeholder colored box in <code>src/scenes/humanLandsScene.ts</code>.</p><ul><li><strong>Budget:</strong> 2564 tris — 64 over the per-asset 2500 target but within the binding v1.2 cap (≤ 3000 tris/object). Kept the clean retextured mesh rather than decimating a <em>textured</em> model (post-texture <code>simplify</code> risks UV-seam artifacts; this is a single static landmark, not an instanced prop, so the 2.5% overage costs nothing at runtime). Bounding box ≈ 1.12 × 2.0 × 1.12 Meshy units — engineering should scale it up in-scene to read as a 3–4 story tower (real-world ≈ 10–14 m tall); up-axis +Y, pivot at base centre.</li><li><strong>Textured (board mandate — not grey):</strong> weathered pale grey stone with cracked stonework, baked into a single 1024² base-color map (JPEG, lighting removed for flat low-poly shading). Geometry-preserving <strong>Meshy retexture</strong> (preferred over PBR refine for low-poly), then textures resized 3.6 MB → 241 KiB via <code>tools/meshy-3d/resize_glb_textures.py --max 1024 --quality 85</code>, taking the GLB from 3.7 MB to <strong>379 KiB</strong> for the web payload budget.</li><li><strong>Provenance:</strong> Meshy text-to-3D preview → retexture. <ul><li>Preview prompt: <em>&quot;low-poly stylized ruined stone watchtower, tall round tower 3-4 stories, cracked broken stonework, partly collapsed top, weathered pale grey stone, faceted flat-shaded, strong vertical silhouette visible from distance, fantasy ruin&quot;</em>.</li><li>Retexture style prompt: <em>&quot;weathered pale grey stone, faceted flat-shaded low-poly, dusty sun-bleached fantasy ruin, cracked broken stonework, muted earthy palette&quot;</em>. Retexture task id <code>019ee749-6051-7990-b286-98be4ecf82b4</code>, 10 Meshy credits.</li></ul></li><li><strong>Rig:</strong> ships <strong>static</strong> (no skeleton) — it is scenery, not animated.</li><li><strong>Verification:</strong> loads headless via <code>node tools/meshy-3d/smoke_load_glb.mjs public/models/watchtower.glb</code> → 2 meshes, 2564 tris, no errors.</li><li><strong>Wiring:</strong> FLO-476 places the GLB in the first forest map as <code>landmark.forest-watchtower</code>, streamed through <code>defaultLoadGlb()</code> with the matte/faceted conform. The canonical Salt Road / human-lands landmark remains a separate scene-wiring concern; this forest placement is the board-requested first-map landmark.</li></ul><h3 id="empire-soldier-enemy-—-phase-2" tabindex="-1">Empire soldier (enemy) — Phase 2 <a class="header-anchor" href="#empire-soldier-enemy-—-phase-2" aria-label="Permalink to &quot;Empire soldier (enemy) — Phase 2&quot;">​</a></h3><p>Napoleonic-era Empire infantryman: bicorne hat, greatcoat, boots, musket held across the body. Generated for <a href="/korovany/plan/game-plan">FLO-311</a> (feeds the enemy-NPC ticket E2.3).</p><ul><li><strong>Budget:</strong> 2794 tris ≤ 3000 (v1.2). Bounding box ≈ 0.97 × 1.90 × 0.84 units (humanoid, ~1.9 tall). Preview-only — no PBR textures (refine is skipped for v1.2; PBR refine drifts the faceted low-poly look into a semi-realistic band).</li><li><strong>Provenance:</strong> Meshy text-to-3D preview, <code>--art-style realistic --target-polycount 3000</code>. Prompt: <em>&quot;low-poly stylized empire soldier enemy, humanoid game character standing in a neutral A-pose, military uniform with peaked helmet and boots, holding a musket rifle, faceted flat-shaded surfaces, muted grey-green palette, full body, single character&quot;</em>. Meshy emitted 3105 tris; welded + decimated to 2794 with <code>gltf-transform</code> (<code>weld</code> → <code>simplify --ratio 0.9 --error 0.01</code>) to fit the budget — silhouette/bbox unchanged. 20 Meshy credits (preview only).</li><li><strong>Rig:</strong> ships <strong>unrigged (static mesh, no skeleton/animation clips)</strong> — this matches the player hero GLB, which is also a static mesh, so the same loader / controller code handles it without special-casing. There is <strong>no hero rig to reuse</strong> (hero has 0 skeletons). E2.3 must therefore drive idle/walk/attack/death either procedurally (transform-level) or by adding a skeletal rig in a separate pass (Meshy auto-rigging is available but was out of scope for this asset-only ticket). Flag rig needs to Pygmalion if a skeletal pass is wanted.</li><li><strong>Verification:</strong> loads headless via <code>node tools/meshy-3d/smoke_load_glb.mjs public/models/empire-soldier.glb</code> → 2 meshes, 2794 tris, no errors.</li></ul><h3 id="ranged-enemy-archer-—-phase-8" tabindex="-1">Ranged enemy (archer) — Phase 8 <a class="header-anchor" href="#ranged-enemy-archer-—-phase-8" aria-label="Permalink to &quot;Ranged enemy (archer) — Phase 8&quot;">​</a></h3><p>A ranged-combat archetype with a <strong>deliberately distinct silhouette from the empire soldier</strong> (the soldier holds a musket across the body; this archer holds a drawn longbow with a back quiver and a hood). Feeds Phase 8 zones and combat depth (<a href="/korovany/FLO/issues/FLO-426">FLO-426</a>, parent <a href="/korovany/plan/game-plan">FLO-423</a>).</p><ul><li><strong>Budget:</strong> 2589 tris ≤ 3000 (v1.2), within the char tier. Bounding box ≈ 0.62 × 2.0 × 1.12 units (humanoid; the bow extends the Z depth — that&#39;s the silhouette). Textured, <strong>357 KiB → 258 KiB</strong> GLB. Up-axis +Y, pivot at base.</li><li><strong>Provenance:</strong> Meshy text-to-3D <strong>preview</strong> at <code>--art-style realistic --target-polycount 2500 --topology triangle</code>, then a geometry-preserving <strong>retexture</strong> (flat/unlit albedo — the v1.2-preferred path over PBR refine, which drifts low-poly into a semi-realistic band, see <code>tools/meshy-3d/API-FITNESS.md</code>). <ul><li>Preview prompt: <em>&quot;low-poly stylized archer enemy, humanoid game character standing drawing a longbow, hooded leather jerkin and soft cap, quiver of arrows on the back, lean ranger build, faceted flat-shaded surfaces, muted earthy palette, full body, single character&quot;</em> (negative: <em>&quot;high-poly, smooth subdivision, photorealistic, gun, musket, rifle&quot;</em>).</li><li>Retexture style: <em>&quot;hooded woodland ranger archer, dark forest-green hood and cloak, brown leather jerkin and bracers, tan leather quiver of arrows, plain wooden longbow, weathered brown boots, flat low-poly unlit faceted color zones, muted earthy forest palette&quot;</em>. Embedded maps resized to ≤1024 via <code>tools/meshy-3d/resize_glb_textures.py --max 1024 --quality 85</code> (2.9 MB → 258 KiB). 20 preview + 10 retexture = 30 Meshy credits.</li></ul></li><li><strong>Rig:</strong> ships <strong>static (no skeleton)</strong> — matches the soldier/hero, so the same loader/controller handles it. A skeletal/animation pass (Meshy auto-rig) is a separate future ticket; flag Pygmalion if wanted.</li><li><strong>Verification:</strong> loads headless via <code>node tools/meshy-3d/smoke_load_glb.mjs public/models/ranged-archer.glb</code> → 2 meshes, 2589 tris, no errors.</li></ul><h3 id="empire-palace-guard-—-phase-8" tabindex="-1">Empire palace-guard — Phase 8 <a class="header-anchor" href="#empire-palace-guard-—-phase-8" aria-label="Permalink to &quot;Empire palace-guard — Phase 8&quot;">​</a></h3><p>An ornate <strong>ceremonial</strong> variant for the Empire zone — crimson dress coat with gold braid and epaulettes, white crossbelts, plumed bicorne — reading clearly distinct from the field soldier&#39;s muted grey-green. <strong>Reuse-first (<a href="/korovany/FLO/issues/FLO-426">FLO-426</a>): no fresh generation</strong> — this is a geometry-preserving <strong>retexture of the existing empire-soldier mesh</strong>, which also gives that shared silhouette a <em>textured</em> delivery (the soldier itself ships preview-only/untextured).</p><ul><li><strong>Budget:</strong> 2793 tris ≤ 3000 (v1.2). Bounding box ≈ 1.01 × 2.0 × 0.88 units — identical geometry to the soldier. Textured, <strong>357 KiB</strong> GLB.</li><li><strong>Provenance:</strong> Meshy <strong>retexture</strong> of the locally-decimated 2794-tri soldier GLB, sourced via a <code>data:</code> URI (not the soldier&#39;s <em>task id</em> — Meshy&#39;s stored task is the undecimated 3105-tri original, which would land over the 3000-tri ceiling; texturing the shipped decimated GLB keeps it in budget without a post-texture decimation that risks UV-seam artifacts). Retexture style: <em>&quot;imperial palace ceremonial guard, ornate crimson red dress coat with gold braid and shoulder epaulettes, white crossbelts over the chest, tall black bearskin ceremonial helmet, polished black boots, flat low-poly unlit faceted color zones, regal imperial crimson and gold palette&quot;</em>. Maps resized to ≤1024 (3.2 MB → 357 KiB). 10 Meshy credits.</li><li><strong>Rig:</strong> ships <strong>static (no skeleton)</strong> — inherits the soldier&#39;s mesh.</li><li><strong>Verification:</strong> loads headless via <code>node tools/meshy-3d/smoke_load_glb.mjs public/models/empire-palace-guard.glb</code> → 2 meshes, 2793 tris, no errors.</li><li><strong>In-scene (Human Lands):</strong> two static ceremonial guards flank the Empire toll gate on the Salt Road, spawned by <code>spawnPalaceGuardProps</code> in <code>src/scenes/palaceGuardProp.ts</code> and wired from <code>humanLandsScene.ts</code> (FLO-471). No AI or combat registration — pure decor. The textured GLB gets the same in-engine <code>flatShade</code> conform as other characters (FLO-452).</li></ul><h3 id="roadside-shrine-salt-road-—-phase-3-5-mpg" tabindex="-1">Roadside shrine (Salt Road) — Phase 3.5 (MPG) <a class="header-anchor" href="#roadside-shrine-salt-road-—-phase-3-5-mpg" aria-label="Permalink to &quot;Roadside shrine (Salt Road) — Phase 3.5 (MPG)&quot;">​</a></h3><p>Humble rural Salt Road landmark (<a href="/korovany/plan/world-specs">world-specs</a> §1), replacing the placeholder colored box in <code>src/scenes/humanLandsScene.ts</code>. Small weathered stone structure with an arched niche, aged-wood back, and a low offering ledge — a readable, distinct silhouette. Generated for <a href="/korovany/plan/game-plan">FLO-375</a>.</p><ul><li><strong>Budget:</strong> 1984 tris ≤ 2000 (within ticket budget), <strong>394 KiB</strong> web payload. Bounding box ≈ 1.52 × 2.00 × 1.15 units (real-world scale, ~2 m tall).</li><li><strong>Textured</strong> (board mandate — never grey): single 1024² base-color map, flat/unlit albedo (worn pale stone with moss accents + aged grey-brown wood).</li><li><strong>Provenance:</strong> Meshy text-to-3D <strong>preview</strong> (<code>019ee73a-…</code>, geometry only, 1985 tris, 20 cr) → <strong>retexture</strong> (<code>019ee747-…</code>, geometry-preserving UV-unwrap <ul><li>paint, 10 cr) → <code>resize_glb_textures.py --max 1024 --quality 85</code> (4.3 MiB → 394 KiB, mesh bytes untouched). Retexture over PBR refine keeps the faceted low-poly silhouette (<a href="#">meshy PBR drift</a> — see <code>API-FITNESS.md</code>). Prompt: <em>&quot;low-poly stylized roadside shrine, small weathered stone structure with an arched niche, simple offering ledge, worn pale stone and aged wood, faceted flat-shaded, humble rural fantasy aesthetic, readable silhouette&quot;</em>; retexture style prompt: <em>&quot;worn pale weathered stone shrine with aged grey-brown wood, humble rural fantasy, flat low-poly muted earthy palette, faceted unlit albedo&quot;</em>.</li></ul></li><li><strong>Rig:</strong> static prop — no skeleton.</li><li><strong>Verification:</strong> loads headless via <code>node tools/meshy-3d/smoke_load_glb.mjs public/models/roadside-shrine.glb</code> → 2 meshes, 1984 tris, 1 embedded texture, no errors.</li><li><strong>Handoff:</strong> wiring this GLB into the scene (replacing the procedural box in <code>humanLandsScene.ts</code>) is engineering&#39;s job — handed to Daedalus/CTO.</li></ul><h3 id="caravan-wagon-salt-road-—-phase-3-5-mpg" tabindex="-1">Caravan wagon (Salt Road) — Phase 3.5 (MPG) <a class="header-anchor" href="#caravan-wagon-salt-road-—-phase-3-5-mpg" aria-label="Permalink to &quot;Caravan wagon (Salt Road) — Phase 3.5 (MPG)&quot;">​</a></h3><p>The raid-loop centerpiece (&quot;грабить корованы&quot;, E3.3; MPG.5 spawns caravans across forest + human-lands). Replaces the procedural brown box stand-in in <code>src/scenes/caravanEnemy.ts</code> (~line 94, <code>CreateBox 1.6 × 1.2 × 2.4</code>). Merchant wagon with a wooden frame, fabric canopy cover, four spoked wheels and a cargo bed — a readable wagon silhouette turns the abstract crate into a tactile target. Generated for <a href="/korovany/plan/game-plan">FLO-371</a>.</p><ul><li><strong>Budget:</strong> 2827 tris ≤ 3000 (v1.2), <strong>394 KiB</strong> web payload. Bounding box ≈ 2.00 × 1.69 × 1.09 units (real-world scale; wagon length runs along <strong>+X</strong>). Note for wiring: the placeholder box is <code>1.6 (w) × 1.2 (h) × 2.4 (d)</code> with the long axis on <strong>Z</strong> — engineering should rotate ~90° about Y and scale to taste so the wagon length aligns with the caravan&#39;s travel axis.</li><li><strong>Textured</strong> (board mandate — never grey): single 1024² base-color map, flat/unlit albedo (weathered tan-brown wood, off-white travel-worn canopy, dark iron-banded wheels).</li><li><strong>Provenance:</strong> Meshy text-to-3D <strong>preview</strong> (<code>019ee749-…</code>, <code>--art-style realistic --target-polycount 3000</code>, 3042 tris, 5 cr) → <code>gltf-transform</code> weld + <code>simplify --ratio 0.93 --error 0.01</code> to 2827 tris (silhouette/bbox unchanged) → <strong>retexture</strong> (<code>019ee74d-…</code>, geometry-preserving UV-unwrap + paint of the decimated mesh via data-URI, 10 cr) → <code>resize_glb_textures.py --max 1024 --quality 85</code> (3.6 MiB → 394 KiB, mesh bytes untouched). Retexture over PBR refine keeps the faceted low-poly silhouette. Preview prompt: <em>&quot;low-poly stylized merchant wagon, wooden frame with fabric canopy cover, four spoked wooden wheels, weathered travel-worn wood and cloth, faceted flat-shaded, neutral tan-brown palette, readable side and front silhouette, fantasy medieval cargo wagon&quot;</em>; retexture style prompt: <em>&quot;weathered tan-brown wooden wagon frame, off-white travel-worn fabric canopy cover, dark iron-banded wheel rims, flat muted low-poly colors, no gloss, fantasy medieval merchant cargo wagon&quot;</em>.</li><li><strong>Rig:</strong> static prop — no skeleton.</li><li><strong>Verification:</strong> loads headless via <code>node tools/meshy-3d/smoke_load_glb.mjs public/models/caravan-wagon.glb</code> → 2 meshes, 2827 tris, 1 embedded texture, no errors.</li><li><strong>Wiring:</strong> connected in FLO-470. <code>CaravanEnemy</code> now keeps the procedural box as the invisible gameplay proxy and mounts this GLB as the visible wagon body, with <code>flatShade()</code> applied at load. The forest manifest also places one static wagon prop near the spawn-side raid route.</li></ul><h3 id="cargo-crate-salt-road-—-phase-3-5-mpg" tabindex="-1">Cargo crate (Salt Road) — Phase 3.5 (MPG) <a class="header-anchor" href="#cargo-crate-salt-road-—-phase-3-5-mpg" aria-label="Permalink to &quot;Cargo crate (Salt Road) — Phase 3.5 (MPG)&quot;">​</a></h3><p>Instanceable static loot prop for caravan cargo clusters and roadside scatter (Salt Road pack). Small weathered wooden crate with iron corner bands — a readable, stackable silhouette for loot piles beside the caravan wagon. Generated for <a href="/korovany/FLO/issues/FLO-372">FLO-372</a>.</p><ul><li><strong>Budget:</strong> 1022 tris ≤ 1200 (ticket budget), <strong>296 KiB</strong> web payload. Bounding box ≈ 2.00 × 1.35 × 1.34 units (real-world scale).</li><li><strong>Textured</strong> (board mandate — never grey): single 1024² base-color map, flat/unlit albedo (weathered brown planks + iron corner bands).</li><li><strong>Provenance:</strong> Meshy text-to-3D <strong>preview</strong> (<code>019ee743-a02c-7878-a22e-7b67cdfbafa6</code>, <code>--art-style realistic</code>, 1022 tris, 20 cr) → geometry-preserving <strong>local</strong> retexture via data-URI (server retexture <code>019ee8d6-…</code> produced a 28k-tri blob and was rejected to preserve the faceted silhouette) → <code>resize_glb_textures.py --max 1024 --quality 85</code> (mesh bytes untouched). Preview prompt: <em>&quot;low-poly stylized wooden cargo crate, rough rectangular planks, iron corner bands, weathered wood, faceted flat-shaded, neutral brown, small stackable game prop&quot;</em>.</li><li><strong>Rig:</strong> static prop — no skeleton.</li><li><strong>Verification:</strong> loads headless via <code>node tools/meshy-3d/smoke_load_glb.mjs public/models/cargo-crate.glb</code> → 2 meshes, 1022 tris, 1 embedded texture, no errors.</li><li><strong>Wiring:</strong> connected in FLO-470 as static forest loot decor via <code>FOREST_CARGO_CRATE_ASSET_ID</code> in the zone streaming manifest.</li></ul><h3 id="forest-leftover-props-—-flo-470" tabindex="-1">Forest leftover props — FLO-470 <a class="header-anchor" href="#forest-leftover-props-—-flo-470" aria-label="Permalink to &quot;Forest leftover props — FLO-470&quot;">​</a></h3><p>FLO-470 wires the remaining visible-but-unused GLBs into the forest scene through the same streaming manifest used by the tree/hut props:</p><ul><li><code>chest.glb</code> → <code>prop.forest-chest</code>, four forest healing chests. Standing within 2.25 m restores 5 HP every 0.5 s; the chests are reusable cooldown stations.</li><li><code>cargo-crate.glb</code> → <code>prop.forest-cargo-crate</code>, one static cargo prop beside the forest wagon camp.</li><li><code>caravan-wagon.glb</code> → <code>prop.forest-caravan-wagon</code>, one prominent static wagon prop; also the live <code>CaravanEnemy</code> visual.</li><li><code>korovany_hero_player-default.glb</code> → <code>npc.forest-static-elf</code>, two static decorative forest elves.</li><li><code>watchtower.glb</code> → <code>landmark.forest-watchtower</code>, one large static ruined tower placed near the first forest map as a visible silhouette landmark (FLO-476).</li></ul><p>All streamed GLBs go through <code>defaultLoadGlb</code>, which normalizes them with <code>loadModel()</code> and applies the shared <code>flatShade()</code> matte/faceted conform before placement so they stay in the v1.2 low-poly visual band.</p>',38)])])}const u=t(s,[["render",d]]);export{p as __pageData,u as default};
