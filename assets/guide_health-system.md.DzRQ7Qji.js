import{_ as t,o as a,c as s,a2 as i}from"./chunks/framework.BYlK8ftn.js";const m=JSON.parse('{"title":"Health system","description":"","frontmatter":{},"headers":[],"relativePath":"guide/health-system.md","filePath":"guide/health-system.md","lastUpdated":1782025478000}'),o={name:"guide/health-system.md"};function d(r,e,n,l,c,h){return a(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="health-system" tabindex="-1">Health system <a class="header-anchor" href="#health-system" aria-label="Permalink to &quot;Health system&quot;">​</a></h1><p>The health system is a pure-function model (<code>src/game/health/</code>) backed by a Redux slice (<code>healthSlice</code>). It tracks the player&#39;s current and maximum hit points and wires death to a menu transition.</p><h2 id="api" tabindex="-1">API <a class="header-anchor" href="#api" aria-label="Permalink to &quot;API&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { applyDamage, createHealth, healDamage, isAlive } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;../game/health&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> hp</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createHealth</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// { current: 100, max: 100 }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> hit</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> applyDamage</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(hp, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">30</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// { current: 70,  max: 100 }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> full</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> healDamage</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(hit, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">30</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// { current: 100, max: 100 }</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">isAlive</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">applyDamage</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(hp, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">9999</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// false</span></span></code></pre></div><p>All functions are <strong>pure</strong> — they return a new <code>HealthState</code> and never mutate the input.</p><h2 id="redux-actions" tabindex="-1">Redux actions <a class="header-anchor" href="#redux-actions" aria-label="Permalink to &quot;Redux actions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Action</th><th>Payload</th><th>Effect</th></tr></thead><tbody><tr><td><code>damagePlayer(n)</code></td><td><code>number</code></td><td>Reduce player HP by <em>n</em>, clamp at 0</td></tr><tr><td><code>healPlayer(n)</code></td><td><code>number</code></td><td>Restore player HP by <em>n</em>, clamp at max</td></tr><tr><td><code>resetPlayerHealth()</code></td><td>—</td><td>Restore player HP to max (100)</td></tr><tr><td><code>restorePlayerHealth(h)</code></td><td><code>HealthState</code></td><td>Replace player HP with a loaded <code>{ current, max }</code> (used by Continue)</td></tr></tbody></table><p>Import from <code>&#39;../store&#39;</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { damagePlayer, healPlayer, resetPlayerHealth, restorePlayerHealth } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;../store&#39;</span></span></code></pre></div><h2 id="hud" tabindex="-1">HUD <a class="header-anchor" href="#hud" aria-label="Permalink to &quot;HUD&quot;">​</a></h2><p>While the game is not in the menu, <code>App.tsx</code> renders a health bar in the HUD (<code>.hud-health</code>): a label, a fill bar whose width tracks <code>current / max</code>, and a numeric <code>current/max</code> readout. The bar is an ARIA <code>group</code> labelled <code>Player health: &lt;current&gt; of &lt;max&gt; hit points</code> for screen readers; the visual bar itself is <code>aria-hidden</code>.</p><p>Alongside it the HUD surfaces the other built-but-previously-invisible systems (MPG.6, FLO-366):</p><ul><li><strong>Bleeding indicator</strong> (<code>.hud-bleeding</code>, ARIA <code>status</code>) — shown while <code>selectIsBleeding</code> is true, with a pulsing dot. The prompt is conditional (P7.2): with no bandage carried it reads &quot;find a bandage&quot;; once a bandage is in the inventory it becomes &quot;press <strong>B</strong> to bandage (N)&quot; — see <a href="#dismemberment-counterplay-p72">Dismemberment counterplay</a> below.</li><li><strong>Score panel</strong> (<code>.hud-score</code>, ARIA <code>group</code> &quot;Score&quot;) — a single inline row <code>Score N · Loot N</code> (P7.5): <strong>Score</strong> (<code>selectScore</code>, the <code>game.score</code> tally) and <strong>Loot</strong> (<code>totalItemCount(inventory)</code>), tabular-nums, separated by a middot to match the objective row above it. Kill increments are wired by the objective loop (MPG.1, FLO-363); the panel surfaces the counter regardless.</li><li><strong>Eye-loss vignette</strong> (<code>.injury-vignette</code>) — a full-viewport, click-through overlay darkening the left half of the screen, rendered (outside the menu) whenever <code>selectHasHalfScreenBlackout</code> is true. It carries no <code>z-index</code>, relying on DOM order to sit above the canvas but below every overlay.</li></ul><h2 id="death-→-menu-transition" tabindex="-1">Death → menu transition <a class="header-anchor" href="#death-→-menu-transition" aria-label="Permalink to &quot;Death → menu transition&quot;">​</a></h2><p><code>App.tsx</code> watches <code>state.health.player.current</code>. When it reaches 0 while the phase is <code>playing</code> or <code>paused</code>, the app dispatches <code>resetPlayerHealth()</code> (so a subsequent New Game starts with full HP) then <code>returnToMenu()</code>.</p><h2 id="save-persistence" tabindex="-1">Save persistence <a class="header-anchor" href="#save-persistence" aria-label="Permalink to &quot;Save persistence&quot;">​</a></h2><p>Player HP survives save/load. The HP travels through the versioned save schema as the <code>health: { current, max }</code> field — see <a href="./save-system">save-system.md</a> for the slot store and <code>SaveData</code> shape.</p><ul><li><strong>Autosave</strong> (on the playing→paused transition) writes the live <code>state.health.player</code> into the save snapshot.</li><li><strong>New Game</strong> dispatches <code>resetPlayerHealth()</code> — always starts at full HP, discarding any saved value.</li><li><strong>Continue</strong> loads the latest save and dispatches <code>restorePlayerHealth(data.health)</code> — the persisted HP is restored exactly.</li></ul><p>Because <code>health</code> is a required field of the current schema (v1), there is no &quot;save without HP&quot;: legacy or malformed records are rejected by <code>parseSaveData</code> and forward-migrated by <code>migrate</code> when the schema version bumps, so a loaded save always carries valid HP.</p><h2 id="injury-dismemberment" tabindex="-1">Injury &amp; dismemberment <a class="header-anchor" href="#injury-dismemberment" aria-label="Permalink to &quot;Injury &amp; dismemberment&quot;">​</a></h2><p>Alongside hit points, the health module models <strong>limb/organ loss</strong> and the three canonical outcomes from the brief (<code>docs/plan/game-plan.md</code> §0). The model is pure (<code>src/game/health/injuryModel.ts</code>) and backed by the <code>injurySlice</code>.</p><h3 id="injury-state" tabindex="-1">Injury state <a class="header-anchor" href="#injury-state" aria-label="Permalink to &quot;Injury state&quot;">​</a></h3><p><code>InjuryState</code> carries a status for each tracked slot plus the bleed timer:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { createInjuryState, severLimb } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;../game/health&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> injuries</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createInjuryState</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// every slot &#39;intact&#39;, not bleeding</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">severLimb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(injuries, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;leftHand&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).bleeding     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// true — a lost hand opens a wound</span></span></code></pre></div><p>Tracked slots (<code>Limb</code>): <code>leftHand</code>, <code>rightHand</code>, <code>leftEye</code>, <code>rightEye</code>, <code>leftLeg</code>, <code>rightLeg</code> — each <code>&#39;intact&#39; | &#39;severed&#39;</code>.</p><h3 id="the-three-outcomes" tabindex="-1">The three outcomes <a class="header-anchor" href="#the-three-outcomes" aria-label="Permalink to &quot;The three outcomes&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Brief</th><th>Trigger</th><th>Modelled as</th></tr></thead><tbody><tr><td><strong>Bleed-out</strong></td><td>lose a hand</td><td><code>bleeding</code> flag → <code>tickInjuries</code> drains HP each second until treated; reaching 0 HP triggers the death → menu transition</td></tr><tr><td><strong>Half-screen</strong></td><td>lose an eye</td><td><code>hasHalfScreenBlackout</code> / <code>selectHasHalfScreenBlackout</code> — true while an eye is severed</td></tr><tr><td><strong>Crawl</strong></td><td>lose a leg</td><td><code>isCrawling</code> / <code>selectLocomotionSpeedMultiplier</code> — speed drops to <code>CRAWL_SPEED_MULTIPLIER</code> (0.35)</td></tr></tbody></table><p>Bleed drains <code>BLEED_DAMAGE_PER_INTERVAL</code> (3) HP every <code>BLEED_INTERVAL_SECONDS</code> (1). A prosthetic/patch (<code>fitProsthetic</code>) restores a slot to intact (clearing the half-screen); <code>treatBleeding</code> stops a bleed without restoring the hand.</p><h3 id="redux-actions-selectors" tabindex="-1">Redux actions &amp; selectors <a class="header-anchor" href="#redux-actions-selectors" aria-label="Permalink to &quot;Redux actions &amp; selectors&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Action</th><th>Payload</th><th>Effect</th></tr></thead><tbody><tr><td><code>severPlayerLimb(limb)</code></td><td><code>Limb</code></td><td>Sever a slot; a hand also starts bleeding</td></tr><tr><td><code>treatPlayerBleeding()</code></td><td>—</td><td>Stop the active bleed</td></tr><tr><td><code>fitPlayerProsthetic(limb)</code></td><td><code>Limb</code></td><td>Restore a slot to intact</td></tr><tr><td><code>advanceBleed(dt)</code></td><td><code>number</code></td><td>Advance the bleed timer (no damage funnelling)</td></tr><tr><td><code>resetInjuries()</code></td><td>—</td><td>Restore every slot to intact</td></tr><tr><td><code>tickInjuries(dt)</code> <em>(thunk)</em></td><td><code>number</code></td><td>Advance bleed <strong>and</strong> funnel damage into <code>health</code></td></tr></tbody></table><p>Selectors: <code>selectInjury</code>, <code>selectIsBleeding</code>, <code>selectHasHalfScreenBlackout</code>, <code>selectIsCrawling</code>, <code>selectLocomotionSpeedMultiplier</code> — all importable from <code>&#39;../store&#39;</code>. As of MPG.6 (FLO-366) these are fully surfaced: the HUD renders the bleeding indicator and eye-loss vignette (see <a href="#hud">HUD</a> above), and the locomotion multiplier reaches movement via <code>CharacterController.getSpeedMultiplier</code>. <code>GameCanvas</code> reads <code>selectLocomotionSpeedMultiplier(store.getState())</code> each step and passes it (through <code>ZoneSceneOptions</code>) to the controller, which scales the per-step move direction — so a severed leg drops the capsule to 35% speed (<code>CRAWL_SPEED_MULTIPLIER</code>).</p><p><code>App.tsx</code> ticks <code>tickInjuries(1)</code> once a second while the player is bleeding and the game is <code>playing</code>, and resets both health and injuries on death.</p><h3 id="combat-→-dismemberment-hook-e6-1-2" tabindex="-1">Combat → dismemberment hook (E6.1.2) <a class="header-anchor" href="#combat-→-dismemberment-hook-e6-1-2" aria-label="Permalink to &quot;Combat → dismemberment hook (E6.1.2)&quot;">​</a></h3><p>Limbs are taken off by combat, not scripted events. The resolver lives in <code>src/game/health/dismemberment.ts</code> — pure functions, randomness injected as an <code>Rng</code> so it is unit-testable:</p><table tabindex="0"><thead><tr><th>Function</th><th>Returns</th><th>Notes</th></tr></thead><tbody><tr><td><code>dismemberChance(amount)</code></td><td><code>number</code></td><td>0 below <code>DISMEMBER_DAMAGE_THRESHOLD</code> (20 HP); ramps <code>DISMEMBER_BASE_CHANCE</code> (0.05) + <code>DISMEMBER_CHANCE_PER_DAMAGE</code> (0.01) per HP over it; capped at <code>DISMEMBER_MAX_CHANCE</code> (0.15)</td></tr><tr><td><code>intactLimbs(state)</code></td><td><code>Limb[]</code></td><td>the slots a hit can still take off</td></tr><tr><td><code>shouldSever(amount, state, rng)</code></td><td><code>boolean</code></td><td>one roll; false when no limbs remain</td></tr><tr><td><code>pickLimb(state, rng)</code></td><td><code>Limb | null</code></td><td>uniform pick among intact limbs</td></tr><tr><td><code>resolveDismemberment(amount, state, rng)</code></td><td><code>Limb | null</code></td><td>combines the two — the limb to sever, or <code>null</code></td></tr></tbody></table><p><code>GameCanvas.onPlayerDamaged</code> calls <code>resolveDismemberment(amount, store.getState().injury, Math.random)</code> right after <code>damagePlayer</code>. On a sever it dispatches <code>severPlayerLimb(limb)</code> — which drives the existing bleed-out / blackout / crawl outcomes — and fires <code>emitDismember(limb)</code> on the combat event bridge (<code>src/game/combat/damageEvents.ts</code>, <code>onDismember</code>) so downstream feedback (audio, HUD) can subscribe without reaching into the scene. Combat is not reproducible, so a plain <code>Math.random</code> generator is used (not the seeded RNG).</p><h3 id="dismemberment-counterplay-p7-2" tabindex="-1">Dismemberment counterplay (P7.2) <a class="header-anchor" href="#dismemberment-counterplay-p7-2" aria-label="Permalink to &quot;Dismemberment counterplay (P7.2)&quot;">​</a></h3><p>The hook shipped its punishment (E6.1.2) before its counterplay. P7.2 closes that gap on two fronts:</p><ol><li><strong>Softened odds.</strong> The constants above were lowered so losing a limb is a rare, dramatic event rather than a routine outcome of every big hit: the threshold rose 15 → 20 HP, the per-hit cap dropped 0.6 → 0.15, and the base/ramp were halved. Ordinary spawn-area blows can no longer maim a fresh player.</li><li><strong>The bandage.</strong> Bleeding can now be <em>stopped by the player</em>. <code>bandage</code> is a real catalog item (<code>BANDAGE_ITEM_ID</code>, <code>src/game/economy/items.ts</code>) that drops from caravans (<code>DEFAULT_CARAVAN_LOOT</code>). The <code>useBandage()</code> thunk (<code>src/store/injurySlice.ts</code>) spends one carried bandage and dispatches <code>treatPlayerBleeding()</code>; it&#39;s a no-op (returns <code>false</code>) when the player isn&#39;t bleeding or has no bandage. <code>App.tsx</code> binds it to the <strong>B</strong> key during play, and the bleeding indicator switches to &quot;press <strong>B</strong> to bandage (N)&quot; once one is carried — so the &quot;find a bandage&quot; prompt now points at an item that exists.</li></ol><h2 id="tests" tabindex="-1">Tests <a class="header-anchor" href="#tests" aria-label="Permalink to &quot;Tests&quot;">​</a></h2><ul><li><code>src/game/health/healthModel.test.ts</code> — 8 unit tests (pure functions)</li><li><code>src/game/health/injuryModel.test.ts</code> — injury model unit tests (pure functions)</li><li><code>src/game/health/dismemberment.test.ts</code> — combat dismemberment resolver (chance curve, limb pick, no-op when maimed)</li><li><code>src/game/combat/damageEvents.test.ts</code> — dismember event pub/sub bridge</li><li><code>src/store/healthSlice.test.ts</code> — 6 Redux tests</li><li><code>src/store/injurySlice.test.ts</code> — injury slice + <code>tickInjuries</code> health-wiring tests</li><li><code>src/app/App.test.tsx</code> — death → menu integration test</li><li><code>src/game/save/save.test.ts</code> — save round-trip preserves <code>health</code> (HP persistence)</li></ul>`,41)])])}const u=t(o,[["render",d]]);export{m as __pageData,u as default};
